{
  "name": "hengdian-portal-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "横店影视 AIGC 门户后端：身份、收藏同步、工具点击埋点与运营统计",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "start": "node --experimental-strip-types src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/**/*.test.ts",
    "seed:tools": "node --experimental-strip-types scripts/sync-tools.ts"
  },
  "dependencies": {
    "hono": "^4.10.3",
    "@hono/node-server": "^1.19.7"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "~5.9.3"
  }
}
